# Database Configuration
# Required for production - PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host:port/database

# Authentication
# Secret key for JWT token generation and verification
JWT_SECRET=your-secure-jwt-secret-key
# Secret key for session management
SESSION_SECRET=your-secure-session-secret-key

# LLM Integration
# Required for chatbot functionality - Groq API key
GROQ_API_KEY=your-groq-api-key

# Application Settings
# Production mode - set to "production" for production environments
NODE_ENV=development
# Set to "1" when deploying to Vercel
VERCEL=

# Frontend settings
# Note: These need to be prefixed with VITE_ to be accessible in the frontend
VITE_APP_NAME=HumanLike-AwareBot
VITE_API_URL=/api